[project]
name = "sample-scripts"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "KostasEreksonas", email = "k.ereksonas@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "dotenv>=0.9.9",
    "requests>=2.34.2",
]

[project.scripts]
sample-scripts = "sample_scripts:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
